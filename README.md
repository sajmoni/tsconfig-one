# tsconfig-lollipop

## Highlights

- Only TypeScript allowed
- As strict as possible
- ESM output
- Fast (incremental)

## Usage

`tsconfig.json`

```json
{
  "extends": "tsconfig-one/tsconfig.json",
  "compilerOptions": {
    "outDir": "dist"
  }
}
```
